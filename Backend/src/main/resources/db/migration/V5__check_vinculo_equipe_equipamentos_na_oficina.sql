-- =========================
-- CHECK VINCULO EQUIPE X EQUIPAMENTOS EM OFICINA
-- =========================
-- Script apenas informativo adaptado para Flyway.
-- Não executa SELECT (Flyway não usa resultados).
-- Mantido como placeholder de validação.

DO $$
BEGIN
    RAISE NOTICE 'V5 - Verificacao de consistencia ignorada em execucao automatica (Flyway)';
END $$;