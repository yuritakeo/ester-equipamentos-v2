# 🚀 Sistema de Equipamentos

Sistema completo composto por:

- Backend (Spring Boot)
- Frontend (Vite)
- Banco PostgreSQL

---

# ⚙️ Requisitos

Antes de começar, instale:

- Docker
- Docker Compose

---

# 🚀 Como rodar o sistema completo

## ✅ 1. Build do backend

> Necessário para gerar o arquivo `.jar`

```bash
cd Backend
./mvnw clean package -DskipTests
cd ..